HTTP server I made to practice my C coding and to try out some things. 

#Usage

```bash
make
./kiwi-http &
```

You can then visit localhost at the port specified in the server.conf file ( the default port is 8080 ). If you want to test it, you can use wrk
